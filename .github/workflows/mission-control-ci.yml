name: Mission Control CI

on:
  push:
    branches:
      - main
      - 'codex/**'
  pull_request:

jobs:
  mission-control-ci:
    name: mission-control-ci
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mission-control-source
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
          cache-dependency-path: mission-control-source/package-lock.json
      - run: npm ci
      - run: npm test
